import { TravelsType } from "../../../types/TravelsType";
import { ThemeInterface } from "../../../interfaces/themeInterface";

export interface TravelProps extends ThemeInterface {
  travel: TravelsType;
}
